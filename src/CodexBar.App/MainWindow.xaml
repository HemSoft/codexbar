<Window x:Class="CodexBar.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CodexBar.App.ViewModels"
        xmlns:local="clr-namespace:CodexBar.App"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}"
        Title="CodexBar"
        Height="600" Width="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- Usage bar style: defaults to green and switches to red when IsHighUsage is true -->
        <Style x:Key="UsageBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="1" />
            <Setter Property="Background" Value="#1A1A2E" />
            <Setter Property="Foreground" Value="#22C55E" />
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsHighUsage}" Value="True">
                    <Setter Property="Foreground" Value="#EF4444" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="12"
            BorderBrush="#3F3F5F"
            BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.4" />
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="🎚️ CodexBar"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,12" />

            <!-- Provider cards (scrollable) -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Providers}">
                    <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}"
                                CornerRadius="8"
                                Padding="12,10"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Provider name -->
                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{Binding DisplayName}"
                                           FontSize="14" FontWeight="Medium"
                                           Foreground="{StaticResource TextPrimaryBrush}" />

                                <!-- Percentage (hidden for credit-based providers) -->
                                <TextBlock Grid.Row="0" Grid.Column="1"
                                           FontSize="13" FontWeight="Medium"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Visibility="{Binding ShowUsagePercent, Converter={StaticResource BoolToVis}}">
                                    <TextBlock.Text>
                                        <Binding Path="UsedPercent" StringFormat="{}{0:P0}" />
                                    </TextBlock.Text>
                                </TextBlock>

                                <!-- Usage bar (hidden for credit-based providers) -->
                                <ProgressBar Grid.Row="1" Grid.ColumnSpan="2"
                                             Value="{Binding UsedPercent, Mode=OneWay}"
                                             Style="{StaticResource UsageBar}"
                                             Visibility="{Binding ShowUsagePercent, Converter={StaticResource BoolToVis}}"
                                             Margin="0,6,0,4" />

                                <!-- Status text -->
                                <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                                           Text="{Binding StatusText}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis" />

                                <!-- Weekly + Reset text -->
                                <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
                                            Margin="0,2,0,0">
                                    <TextBlock Text="{Binding WeeklyText}"
                                               FontSize="10"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Visibility="{Binding WeeklyText, Converter={x:Static local:NullToCollapsedConverter.Instance}}" />
                                    <TextBlock Text="{Binding ResetText}"
                                               FontSize="10"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Visibility="{Binding ResetText, Converter={x:Static local:NullToCollapsedConverter.Instance}}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,8,0,0">
                <TextBlock Text="v0.1.0"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="11" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
